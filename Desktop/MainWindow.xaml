<Window
    x:Class="Desktop.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Desktop"
    Title="Embedeb Desktop">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <StackPanel Orientation="Vertical" Spacing="20">

        <TextBlock Text="A TextBloc"/>
        <Button Content="A Button"/>

        <DropDownButton Content="A DropDownButton ">
            <DropDownButton.Flyout>
                <MenuFlyout Placement="Bottom">
                    <MenuFlyoutItem Text="First option"/>
                    <MenuFlyoutItem Text="Second option"/>
                    <MenuFlyoutItem Text="Third option"/>
                </MenuFlyout>
            </DropDownButton.Flyout>
        </DropDownButton>
    </StackPanel>
    
</Window>
