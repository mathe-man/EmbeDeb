<UserControl x:Class="Desktop.Views.EventView"
             
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             
             xmlns:local="clr-namespace:Desktop.Views"
             
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <DataGrid ItemsSource="{Binding RecentEvents}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  GridLinesVisibility="Vertical">


            <DataGrid.Columns>

                <DataGridTextColumn Header="Event"
                                    Binding="{Binding EventName}"
                                    Width="3*"/>

                <DataGridTextColumn Header="Report Time"
                                    Binding="{Binding ReportTime}"
                                    Width="2*"/>

            </DataGrid.Columns>
        </DataGrid>
                  
    </Grid>
</UserControl>
